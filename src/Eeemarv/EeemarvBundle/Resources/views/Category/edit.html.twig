{% extends 'EeemarvBundle::base.html.twig' %}
{% form_theme form 'EeemarvBundle:Form:fields.html.twig' %}


{% block page_header_inner %}{{ 'category_edit'|trans }}{% endblock %}


{% block inner_content -%}

    <form action="{{ path('eeemarv_eeemarv_category_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(form) }}
        <p>
            <button type="submit" class="btn btn-primary">{{ 'edit'|trans }}</button>
        </p>
    </form>
{#
        <ul class="record_actions">
    <li>
        <a href="{{ path('eeemarv_eeemarv_category_index') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('eeemarv_eeemarv_category_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
#}
{% endblock %}
